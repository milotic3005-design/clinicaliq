## 2024-04-20 - Clear Search Input Button
**Learning:** Adding a clear button to search inputs significantly improves keyboard navigation and overall user experience for iterative searching.
**Action:** Always include a keyboard accessible clear button with a visible focus state and appropriate `aria-label` when designing search input components. Ensure it gracefully replaces loading indicators to avoid layout shift.
