## 2024-07-07 - Search Clear Button Focus
**Learning:** When implementing elements that remove user input, focus must be explicitly restored to the input to prevent screen readers and keyboard users from losing context.
**Action:** Always include focus restoration logic, clear stale autocomplete state, and dynamically adjust input padding to prevent text overlap.
